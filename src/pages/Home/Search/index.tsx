import styled from 'styled-components';

const Container = styled.div`
  flex-grow: 1;
`;

const Search = () => {
  return (
    <Container>
      <div>Search</div>
    </Container>
  );
};

export default Search;
